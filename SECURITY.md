# Security note

The original uploaded HTML contained a Gemini API key. The APK project intentionally removes that key before packaging.

If the original key has ever been committed to GitHub or shared publicly, revoke/rotate it in Google AI Studio and issue a new key.
Do not commit the replacement key to the repository.
